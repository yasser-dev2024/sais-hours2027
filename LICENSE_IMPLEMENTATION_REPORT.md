# تقرير تنفيذ التعهد والتجربة والتفعيل

## النتيجة

أضيفت طبقة دخول مستقلة قبل تهيئة وظائف التطبيق الحالي، وفق التعديل النهائي
للمدة: **تجربة كاملة لمدة 20 يومًا** ثم قفل الدخول إلى أن يتم إدخال ترخيص
دائم صحيح مرتبط بالتطبيق وبـ Installation ID.

## الملفات الجديدة

- `lib/license/`: إعداد الترخيص، النماذج، التخزين الآمن، Installation ID،
  إدارة التعهد، احتساب التجربة، التحقق من Ed25519، الخدمة، بوابة الدخول،
  وشاشتا التعهد والتفعيل.
- `test/license/`: اختبارات منطق الترخيص وبوابة الواجهة.
- `developer_tools/license_generator/`: أداة المطور المنفصلة لتوليد تراخيص
  دائمة موقعة بـ Ed25519 وتعليمات استخدامها.
- `docs/downloads/SaisAlkhail-v2.1.1-build22.apk`: نسخة Release النهائية
  الخاصة بصفحة التنزيل.

المفتاح الخاص غير موجود داخل المستودع أو APK. على جهاز المطور الحالي يُحفظ
خارج المشروع تحت `%LOCALAPPDATA%\HorseManagerLicenseGenerator` ومحمي بواسطة
Windows DPAPI. يحتوي التطبيق على المفتاح العام فقط.

## الملفات الحالية المعدلة

- `lib/main.dart`: تأخير تهيئة منطق التطبيق إلى ما بعد سماح بوابة الترخيص.
- `lib/app/app.dart`: وضع بوابة الترخيص قبل التطبيق والتنبيهات والتنقل الحالي.
- `pubspec.yaml` و`pubspec.lock`: إضافة التحقق Ed25519 والتخزين الآمن.
- `integration_test/app_test.dart`: إدخال قبول التعهد ضمن مسار اختبار التجربة.
- `docs/index.html` و`docs/assets/style.css`: صفحة تنزيل بعنوان «سايس الخيل
  الرقمي»، مع عرض المزايا والنقلة النوعية الرقمية ومدة التجربة.
- `docs/README.txt` و`build_android_and_web.ps1`: مسار APK مرقّم وتحديث آلي
  لبيانات الإصدار والحجم والبصمة. كانت هذه التعديلات موجودة محليًا قبل بدء
  إضافة الترخيص، وتم الحفاظ عليها وضمها للإصدار.
- حُذف اسم APK القديم `docs/downloads/HorseClub.apk` واستُبدل بالملف المرقّم؛
  ويمكن استعادة القديم من سجل Git.

## الأمان والسلوك

- يبدأ توقيت التجربة قبل عرض التعهد، فلا يؤخر عدم القبول بداية الأيام.
- الموافقة غير محددة مسبقًا، ولا يعمل زر المتابعة قبل تحديدها، وتحفظ منفصلة
  عن السيريال.
- تحفظ الحالة في Android Keystore/EncryptedSharedPreferences، مع تعطيل نسخ
  Android الاحتياطي الموجود أصلًا في Manifest.
- يُحفظ آخر وقت موثوق، ويؤدي إرجاع ساعة الجهاز إلى الخلف إلى القفل.
- يُعاد التحقق من التوقيع وApp ID وInstallation ID ونوع `lifetime` عند كل
  تشغيل؛ والحالة الناتجة هي `ACTIVATED_LIFETIME`.
- يعمل التفعيل دون اتصال ولا يوجد Master Password أو Private Key داخل APK.
- شاشة القفل تعرض واتساب فقط على الرقم `0501894192` لطلب التفعيل.

## نتائج التحقق

- `flutter analyze`: نجح دون ملاحظات.
- `flutter test`: نجحت الاختبارات الثلاثون كلها، ومنها التثبيت المنطقي الجديد،
  بدء 20 يومًا قبل التعهد، الرفض والقبول، الانتهاء، إرجاع الساعة، الرموز
  الخاطئة والصحيحة، جهاز/تطبيق آخر، العبث، الاستعادة بعد إعادة التشغيل أو
  التحديث، والعمل بلا شبكة.
- تحقق توافق مباشر بين رمز أنشأته أداة Python والتحقق داخل Flutter.
- APK Release: معرّف الحزمة والإصدار كما هما، توقيع v2 صحيح وبصمة شهادة
  Release الحالية مطابقة للنسخة السابقة، والمحاذاة صحيحة.
- ثُبت APK على الهاتف الموصول كتحديث محافظ دون حذف البيانات، وظهر التعهد
  فعليًا. لم تُضغط الموافقة آليًا لأنها إقرار يجب أن ينفذه المستخدم بنفسه.
- لم تتغير ملفات قاعدة البيانات أو مخططها أو الشاشات والوظائف الأصلية أو
  الألوان والتنقل؛ التعديل القديم الوحيد داخل واجهة التطبيق هو جذر الدخول.

## حد تقني يجب معرفته

يحافظ التخزين الآمن على التجربة والتفعيل عند إغلاق التطبيق وإعادة تشغيل
الجهاز والتحديث فوق النسخة الحالية. لكن أي تطبيق Offline بالكامل لا يستطيع
منع إعادة ضبط الحالة بنسبة مطلقة بعد **حذف بيانات التطبيق أو إلغاء تثبيته**؛
Android يحذف مخزن التطبيق ومفاتيح Keystore عندها. منع ذلك كليًا يتطلب خدمة
ترخيص خارجية أو حسابًا موثوقًا، وهو يتعارض مع شرط العمل Offline دون اتصال دائم.
